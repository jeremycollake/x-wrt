#!/bin/sh
tar -cf rom/local.tar --owner=root --group=root etc
mkdir -p tmp/local/etc
chmod 0755 tmp/local
chmod 0755 tmp/local/etc
mv etc/preinit tmp/local/etc/preinit
rm -rf etc
ln -s /tmp/local/etc etc
ln -s /tmp/local usr/local
