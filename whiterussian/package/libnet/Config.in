config BR2_PACKAGE_LIBNET0
	tristate "libnet0 - Low-level packet creation library"
	default m if CONFIG_DEVEL
	select BR2_PACKAGE_LIBPCAP
	help
	  A low-level packet creation library.
	  
	  http://www.packetfactory.net/libnet/
	  
	  Depends: libpcap
	  

